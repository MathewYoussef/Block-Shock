# Results

- `raw/`: JSONL per run
- `tables/`: aggregated CSVs
- `plots/`: charts produced by analysis scripts

Notes:

- Phase 2 skips Block-Shock because semi-structured sparse backward is not supported in the current PyTorch build.
