### block_shock/src/orchestrator.py
## Phase pipelines for phases 0-3.
# Orchestrates build, forward, backward, step, validate, and timing.

#TODO: define per-phase pipelines and enabled steps
#TODO: call method.build(), forward(), backward(), step()
#TODO: wire correctness checks and timing regions


def run_phase(_cfg, _method, _inputs):
    #TODO: branch on phase and execute pipeline
    raise NotImplementedError("Scaffold only: implement phase orchestration.")
