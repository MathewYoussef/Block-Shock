# Phase 1: forward-only benchmark.

phase:
  name: phase1_forward
  forward_only: true
  check_correctness: false
  check_grad_input: false
  train_step: false
  record_timings: true
  warmup_iters: 10
  timed_iters: 100
