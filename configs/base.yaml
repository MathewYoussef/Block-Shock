# Base experiment defaults. Override via phase/method/workload/hardware configs.

experiment:
  name: block_shock
  seed: 725

model:
  dtype: bf16
  N: 4096
  B: 64

phase:
  name: phase0_correctness
  warmup_iters: 10
  timed_iters: 100
  tol_max_abs: 64.0
  tol_max_rel: 64.0

method:
  name: dense_single

mask:
  name: complement_1100_0011

workload:
  name: gaussian
  seed: 725

hardware:
  name: local_2gpu

logging:
  out_dir: results/raw
  table_dir: results/tables
  plot_dir: results/plots
